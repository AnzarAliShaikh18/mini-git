# Header files

This folder is reserved for C++ headers (.h or .hpp) as Mini Git grows.
Day 1 uses only src/main.cpp, so no header is needed yet.

This note also keeps the folder visible on GitHub: Git tracks files, not empty folders.
